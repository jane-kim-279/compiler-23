#include <stdio.h>
#include "tn.h"
#include "global.h"

void main() {
	enum tnumber tn;
	//print header

	//**code from prof**

	//while ((tn = yylex()) != EOF) {
	//	printtoken(tn);
	//}
	//if (cErrors == 0) then
	//	printf("No errors defected ")
	//	Else
	//	printf("%d errors detected", cErrors);
	//end if
}